#pragma once

namespace backtest {

struct Config {
    // Add configuration parameters here if needed in the future
    // For now, it can be an empty struct as we are removing its direct usages
};

}
